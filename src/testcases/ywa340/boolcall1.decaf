extern func print_int(int) void;

package Test {
    func test(a bool) void {
        if (a) { print_int(1); }
        if (!a) { print_int(2); }
    }
    func main() int {
        test(true);
        test(false);
    }
}
